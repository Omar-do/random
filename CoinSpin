using UnityEngine;

public class CoinSpin : MonoBehaviour
{
    public float rotationSpeed = 100f; // سرعة الدوران

    void Update()
    {
        // يخلي الكوين تلف حول نفسها على محور Y
        transform.Rotate(Vector3.up * rotationSpeed * Time.deltaTime, Space.Self);
    }
}
